import React from "react";

interface Props {
  name?: string;
  placeholder?: string;
  rows?: number;
  cols?: number;
  disabled?: boolean;
  readonly?: boolean;
  maxlength?: number;
  autofocus?: boolean;
}

const Textarea = (props: Props) => {
  return <div></div>;
};

export default Textarea;
